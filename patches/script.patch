--- a/public/script.js
+++ b/public/script.js
@@ -72,6 +72,7 @@ import {
     getGroupBlock,
     getGroupCharactersLazy,
     getGroupDepthPrompts,
+    resetGroupIncrementalSaveState,
 } from './scripts/group-chats.js';
 
 import {
@@ -388,6 +389,66 @@ export let swipeState = SWIPE_STATE.NONE;
 let chatSaveTimeout;
 let importFlashTimeout;
 
+// Image proxy: intercept Image.src setter to route external images through the caching proxy
+(function installImageProxyInterceptor() {
+    const descriptor = Object.getOwnPropertyDescriptor(HTMLImageElement.prototype, 'src');
+    if (!descriptor || !descriptor.set) return;
+    const originalSet = descriptor.set;
+    const origin = window.location.origin;
+    Object.defineProperty(HTMLImageElement.prototype, 'src', {
+        ...descriptor,
+        set(value) {
+            if (typeof value === 'string' && (value.indexOf('://') > 0 || value.indexOf('//') === 0) && !value.startsWith(origin) && !value.startsWith('/api/image-proxy')) {
+                value = '/api/image-proxy?url=' + encodeURIComponent(value);
+            }
+            originalSet.call(this, value);
+        },
+    });
+    console.debug('[ImageProxy] Image.src interceptor installed');
+})();
+
+// Incremental save tracking
+let lastSavedChatName = null;
+let lastSavedChatLength = 0;
+let lastSavedChatHash = null;
+
+/**
+ * Computes a lightweight hash of chat messages for change detection.
+ * @param {object[]} chatArray The chat array
+ * @param {number} upToIndex Hash messages up to this index
+ * @returns {string} Hash string
+ */
+function computeChatHash(chatArray, upToIndex) {
+    let hash = '';
+    // Start from index 1: skip the first message (greeting) because Generate()
+    // calls substituteParams(chat[0].mes) which may change its length every time
+    for (let i = 1; i < upToIndex && i < chatArray.length; i++) {
+        const m = chatArray[i];
+        hash += `${(m.mes || '').length}:${m.swipe_id ?? 0},`;
+    }
+    return hash;
+}
+
+/**
+ * Checks if old messages have been modified since last save.
+ * @param {object[]} chatArray Current chat array
+ * @param {number} savedLength Number of messages at last save
+ * @returns {boolean} True if old messages changed
+ */
+function hasOldMessagesChanged(chatArray, savedLength) {
+    return computeChatHash(chatArray, savedLength) !== lastSavedChatHash;
+}
+
+/**
+ * Resets incremental save tracking state.
+ * Call this when switching chats, characters, or reloading.
+ */
+export function resetIncrementalSaveState() {
+    lastSavedChatName = null;
+    lastSavedChatLength = 0;
+    lastSavedChatHash = null;
+}
 export let isChatSaving = false;
 let firstRun = false;
 export let settingsReady = false;
@@ -9303,7 +9364,12 @@ export async function saveChatConditional() {
         saveItemizedPrompts(getCurrentChatId());
     } catch (error) {
         console.error('Error saving chat', error);
     } finally {
         isChatSaving = false;
     }
+
+    // Reset incremental save tracking when chat data is cleared
+    if (clearData) {
+        resetIncrementalSaveState();
+        resetGroupIncrementalSaveState();
+    }
 }
 
 export async function deleteLastMessage() {
@@ -7330,6 +7386,50 @@ export async function saveChat({ chatName, withMetadata, mesId, force = false, c
     };
 
     try {
+        // ==== Incremental save check ====
+        const isSameChat = (
+            !force &&
+            mesId === undefined &&
+            fileName === lastSavedChatName &&
+            lastSavedChatLength > 0
+        );
+
+        const hasNewMessages = isSameChat && trimmedChat.length > lastSavedChatLength;
+        const isSameLength = isSameChat && trimmedChat.length === lastSavedChatLength;
+        const oldMessagesUnchanged = isSameChat && !hasOldMessagesChanged(trimmedChat, lastSavedChatLength);
+
+        // Case 1: New messages appended + old messages unchanged → incremental append
+        // Case 2: Same length + old messages unchanged → header-only update (metadata save)
+        const canAppend = hasNewMessages && oldMessagesUnchanged;
+        const canHeaderOnly = isSameLength && oldMessagesUnchanged;
+
+        if (canAppend || canHeaderOnly) {
+            const newMessages = canAppend ? trimmedChat.slice(lastSavedChatLength) : [];
+            const debugAction = canAppend ? `appending ${newMessages.length} new message(s)` : 'header-only update';
+            console.debug(`Incremental save: ${debugAction} to "${fileName}"`);
+            const appendResult = await fetch('/api/chats/save-append', {
+                method: 'POST',
+                cache: 'no-cache',
+                headers: getRequestHeaders(),
+                body: JSON.stringify({
+                    avatar_url: characters[this_chid].avatar,
+                    file_name: fileName,
+                    header: chatHeader,
+                    newMessages,
+                    expectedLines: lastSavedChatLength + 1, // +1 for header line
+                }),
+            });
+
+            if (appendResult.ok) {
+                lastSavedChatLength = trimmedChat.length;
+                lastSavedChatHash = computeChatHash(trimmedChat, trimmedChat.length);
+                return;
+            }
+            // If incremental save failed (409 conflict), fall through to full save
+            console.warn('Incremental save failed, falling back to full save');
+        }
+
         const saveChatRequest = await compressRequest({
             method: 'POST',
             cache: 'no-cache',
